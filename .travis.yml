language: ruby
rvm:
  - 2.3.0
install: gem install jekyll && gem install s3_website && gem install html-proofer
script:
  - bundle exec jekyll build
  - bundle exec htmlproof ./_site --only-4xx --check-favicon --check-html
env:
  global:
    - NOKOGIRI_USE_SYSTEM_LIBRARIES=true # speeds up installation of html-proofer
after_success: travis_retry bundle exec s3_website push --headless
sudo: false
